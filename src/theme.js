import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  status: {
    danger: '#e53e3e',
  },
  palette: {
    primary: {
      main: '#163947',
    },
    secondary: {
      main: '#F3D3B8',
    },
    tertiary: {
      main: '#CAC6A0',
    },
  },
});

export default theme;
