import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  status: {
    danger: '#e53e3e',
  },
  palette: {
    primary: {
      main: '#2F496E',
    },
    secondary: {
      main: '#F4EADE',
    },
    tertiary: {
      main: '#ED8C72f',
    },
  },
});

export default theme;
